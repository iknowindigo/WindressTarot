export interface TarotCard {
    id: string;
    title: string;
    imageUrl: string;
    FaceDownImageUrl: string;
    key: string;    
    cardSelected: boolean;
    comment: string;
    thoughts: string;
}